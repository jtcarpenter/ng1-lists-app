'use strict';

describe('ListsController', function () {

  // load the controller's module
  //angular.module('myApp.services') instead of just module('myApp.services')
  // beforeEach(angular.module('listsApp'));

  // var ListsCtrl,
  //   scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   ListsCtrl = $controller('ListsController', {
  //     $scope: scope
  //   });
  // }));
  it('to work correctly', function() {
    var x = 10;
    var y = 10;
    expect(x).toBe(y);
  });
});
